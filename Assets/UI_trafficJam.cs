using UnityEngine;
using UnityEngine.UI;

public class UI_trafficJam : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    private bool trafficJam;

    private WholeTunnel wTunnel;
    public Button jamButton;

    private GameObject theTunnel;
    // Pat din hat husk funktionr
    //wTunnel = theTunnel.GetComponent<WholeTunnel>;

}
